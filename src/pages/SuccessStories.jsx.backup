import React from 'react';
import { FaStar, FaQuoteLeft } from 'react-icons/fa';
import { successStories } from '../data/companyProfile';

const SuccessStories = () => {
    const stories = successStories;
        },
        {
            name: 'Anjali Mehta',
            business: 'E-commerce Platform, Delhi',
            funding: '₹40 Lakhs',
            scheme: 'Startup India',
            image: '🛒',
            story: 'Built an e-commerce platform for handmade products. The Startup India recognition and funding opened many doors. GMV crossed ₹5 Crores last year.',
            rating: 5
        }
    ];

    return (
        <div className="min-h-screen">
            {/* Hero Section */}
            <section className="bg-gradient-to-r from-blue-600 to-purple-600 text-white py-20">
                <div className="container mx-auto px-4">
                    <div className="max-w-4xl mx-auto text-center">
                        <h1 className="text-5xl font-bold mb-6">Success Stories</h1>
                        <p className="text-xl text-gray-100">
                            Real businesses, real success. See how we've helped entrepreneurs achieve their dreams.
                        </p>
                    </div>
                </div>
            </section>

            {/* Stats Section */}
            <section className="py-12 bg-white">
                <div className="container mx-auto px-4">
                    <div className="grid md:grid-cols-4 gap-8 text-center">
                        <div>
                            <p className="text-4xl font-bold text-blue-600 mb-2">20,000+</p>
                            <p className="text-gray-600">Businesses Funded</p>
                        </div>
                        <div>
                            <p className="text-4xl font-bold text-purple-600 mb-2">₹500Cr+</p>
                            <p className="text-gray-600">Total Funding</p>
                        </div>
                        <div>
                            <p className="text-4xl font-bold text-green-600 mb-2">95%</p>
                            <p className="text-gray-600">Success Rate</p>
                        </div>
                        <div>
                            <p className="text-4xl font-bold text-pink-600 mb-2">4.9/5</p>
                            <p className="text-gray-600">Customer Rating</p>
                        </div>
                    </div>
                </div>
            </section>

            {/* Success Stories Grid */}
            <section className="py-16 bg-gray-50">
                <div className="container mx-auto px-4">
                    <h2 className="text-4xl font-bold text-center mb-12">Featured Success Stories</h2>

                    <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                        {stories.map((story, index) => (
                            <div key={index} className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transition">
                                {/* Card Header */}
                                <div className="bg-gradient-to-r from-blue-500 to-purple-500 p-6 text-white">
                                    <div className="flex items-center mb-4">
                                        <div className="w-16 h-16 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center text-3xl mr-4">
                                            {story.image}
                                        </div>
                                        <div>
                                            <h3 className="text-xl font-bold">{story.name}</h3>
                                            <p className="text-sm text-gray-200">{story.business}</p>
                                        </div>
                                    </div>

                                    <div className="flex justify-between items-center">
                                        <div>
                                            <p className="text-sm text-gray-200">Funding</p>
                                            <p className="text-2xl font-bold">{story.funding}</p>
                                        </div>
                                        <div className="bg-white/20 backdrop-blur-sm px-3 py-1 rounded-full text-sm">
                                            {story.scheme}
                                        </div>
                                    </div>
                                </div>

                                {/* Card Body */}
                                <div className="p-6">
                                    <FaQuoteLeft className="text-blue-600 text-2xl mb-4" />
                                    <p className="text-gray-700 mb-4 italic">"{story.story}"</p>

                                    {/* Rating */}
                                    <div className="flex items-center">
                                        {[...Array(story.rating)].map((_, i) => (
                                            <FaStar key={i} className="text-yellow-400 mr-1" />
                                        ))}
                                        <span className="ml-2 text-gray-600 text-sm">({story.rating}.0)</span>
                                    </div>
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            </section>

            {/* Video Testimonials Section */}
            <section className="py-16 bg-white">
                <div className="container mx-auto px-4">
                    <h2 className="text-4xl font-bold text-center mb-12">Video Testimonials</h2>

                    <div className="grid md:grid-cols-3 gap-8">
                        {[1, 2, 3].map((item, index) => (
                            <div key={index} className="bg-gray-200 rounded-xl h-64 flex items-center justify-center">
                                <div className="text-center">
                                    <div className="w-16 h-16 bg-blue-600 rounded-full mx-auto mb-4 flex items-center justify-center text-white text-2xl">
                                        ▶
                                    </div>
                                    <p className="text-gray-600">Video Testimonial {item}</p>
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            </section>

            {/* Industry-wise Success */}
            <section className="py-16 bg-gradient-to-br from-blue-50 to-purple-50">
                <div className="container mx-auto px-4">
                    <h2 className="text-4xl font-bold text-center mb-12">Success Across Industries</h2>

                    <div className="grid md:grid-cols-3 gap-8">
                        {[
                            { industry: 'Manufacturing', count: '6,500+', icon: '🏭' },
                            { industry: 'Technology', count: '3,200+', icon: '💻' },
                            { industry: 'Retail & Trading', count: '5,800+', icon: '🏪' },
                            { industry: 'Food & Beverage', count: '2,100+', icon: '🍽️' },
                            { industry: 'Healthcare', count: '1,400+', icon: '🏥' },
                            { industry: 'Services', count: '4,000+', icon: '🎯' }
                        ].map((item, index) => (
                            <div key={index} className="bg-white rounded-xl p-8 text-center shadow-lg hover:shadow-xl transition">
                                <div className="text-5xl mb-4">{item.icon}</div>
                                <h3 className="text-2xl font-bold mb-2">{item.industry}</h3>
                                <p className="text-3xl font-bold text-blue-600">{item.count}</p>
                                <p className="text-gray-600">Businesses Funded</p>
                            </div>
                        ))}
                    </div>
                </div>
            </section>

            {/* CTA Section */}
            <section className="py-16 bg-gradient-to-r from-blue-600 to-purple-600 text-white">
                <div className="container mx-auto px-4 text-center">
                    <h2 className="text-4xl font-bold mb-6">Be Our Next Success Story</h2>
                    <p className="text-xl mb-8">Join thousands of successful entrepreneurs who trusted us with their funding needs</p>
                    <button className="bg-white text-blue-600 px-12 py-4 rounded-lg font-bold text-lg hover:shadow-2xl transition transform hover:scale-105">
                        Apply Now
                    </button>
                </div>
            </section>
        </div>
    );
};

export default SuccessStories;
